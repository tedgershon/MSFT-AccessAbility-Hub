[project]
name = "__SERVICE_ID__"
version = "0.1.0"
description = "TODO: describe the __SERVICE_NAME__ service."
requires-python = ">=3.11"
dependencies = ["aah-contracts"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["__PY_PKG__"]
