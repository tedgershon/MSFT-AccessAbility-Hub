{
  "name": "@aah/shell",
  "version": "0.1.0",
  "description": "Electron host: toggle UI, status, and mode-switch UI. Bootstraps the kernel.",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -b && tsc -p tsconfig.renderer.json",
    "dev": "electron .",
    "typecheck": "tsc -b && tsc -p tsconfig.renderer.json",
    "test": "vitest run",
    "lint": "echo \"(lint placeholder)\""
  },
  "dependencies": {
    "@aah/contracts": "workspace:*",
    "@aah/coordinator": "workspace:*",
    "@aah/kernel": "workspace:*",
    "@aah/colorblind-contrast": "workspace:*",
    "@aah/artinsight": "workspace:*",
    "@aah/privacy-guard": "workspace:*",
    "@aah/adaptive-learning": "workspace:*",
    "@aah/display-capture": "workspace:*",
    "@aah/tts": "workspace:*",
    "@aah/flash-filter": "workspace:*",
    "@aah/creative-studio": "workspace:*",
    "@aah/ipc": "workspace:*"
  },
  "devDependencies": {
    "electron": "^30.0.0"
  }
}