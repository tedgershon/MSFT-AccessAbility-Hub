{
  "name": "@aah/integration-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-cutting and TS<->Python IPC-seam integration tests. Contains tests only.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@aah/contracts": "workspace:*",
    "@aah/ipc": "workspace:*",
    "@aah/kernel": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.6.0"
  }
}
