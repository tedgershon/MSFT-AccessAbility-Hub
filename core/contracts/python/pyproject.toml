[project]
name = "aah-contracts"
version = "0.1.0"
description = "Python mirror of the AccessAbility Hub shared contracts."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["aah_contracts"]
