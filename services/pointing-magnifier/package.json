{
  "name": "@aah/pointing-magnifier",
  "version": "0.1.0",
  "description": "Cursor magnification / target-assist overlay for motor & dexterity (issue #22) — pure display overlay, runs in the shell.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "echo \"(lint runs from the repo root: pnpm lint)\""
  },
  "dependencies": {
    "@aah/contracts": "workspace:*"
  },
  "devDependencies": {
    "@aah/test-fixtures": "workspace:*"
  }
}
