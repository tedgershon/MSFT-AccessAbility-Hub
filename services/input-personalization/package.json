{
  "name": "@aah/input-personalization",
  "version": "0.1.0",
  "description": "Per-user input remapping profile (issue #25) for motor & dexterity — settings store, no direct OS injection.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "echo \"(lint runs from the repo root: pnpm lint)\""
  },
  "dependencies": {
    "@aah/contracts": "workspace:*"
  },
  "devDependencies": {
    "@aah/test-fixtures": "workspace:*"
  }
}
