{
  "name": "@aah/colorblind-contrast",
  "version": "0.1.0",
  "description": "Colorblind / contrast correction — pure display overlay, runs in the shell.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "echo \"(lint placeholder)\""
  },
  "dependencies": {
    "@aah/contracts": "workspace:*"
  },
  "devDependencies": {
    "@aah/test-fixtures": "workspace:*"
  }
}
